import Sidebar from "@/components/SliderBar";

const HomePage: React.FC = () => {
  return (
    <div style={{ backgroundColor: "black", minHeight: "1500px" }}>
      <Sidebar />
    </div>
  );
};

export default HomePage;
